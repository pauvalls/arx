# Tasks: Cross-Language Dependency Resolution

## Review Workload Forecast

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: Low

~200–250 changed lines across 5 files (3 new/modified source + 2 test). Well under the 400-line review budget.

## Phase 1: Dependency Model

- [x] 1.1 Add `Language string` field to `domain.Dependency` in `internal/domain/dependency.go` — omitempty yaml/json tag
- [x] 1.2 Set `Language` in all 10 detectors' `ExtractImports`: Go→`"go"`, TS→`"typescript"`, Python→`"python"`, Java→`"java"`, Kotlin→`"kotlin"`, Rust→`"rust"`, C#→`"csharp"`, Ruby→`"ruby"`, Swift→`"swift"`, PHP→`"php"`
- [x] 1.3 Update test factories and inline `domain.Dependency{}` inits across `internal/infrastructure/detector/**/*_test.go` that create deps without `Language`

## Phase 2: Config Model

- [x] 2.1 Add `CrossLanguage *CrossLanguageConfig` field to `domain.Config` in `internal/domain/config.go` (pointer for nil-zero config backward compat)
- [x] 2.2 Define `CrossLanguageConfig` (with `Mappings []CrossLanguageMapping`) and `CrossLanguageMapping{SourcePattern, GeneratedPattern, Language string}` types
- [x] 2.3 Validate mappings in `Config.Validate()` — reject empty patterns, validate glob syntax with `filepath.Match` on a test string

## Phase 3: Cross-Language Detector

- [ ] 3.1 Create `internal/infrastructure/detector/crosslanguage/detector.go` — `CrossLanguageDetector` struct holding `*domain.CrossLanguageConfig`, implement `ports.Detector`
- [ ] 3.2 `Name()` returns `"cross-language"`; `Detect()` returns `len(cfg.Mappings) > 0`
- [ ] 3.3 `ExtractImports()`: glob each mapping's `source_pattern`, extract base name (stem), glob `generated_pattern`, filter by stem match
- [ ] 3.4 Header verification: read first line of each candidate generated file, match `// Code generated by protoc-gen-`
- [ ] 3.5 Emit `Dependency{Language: "cross", SourceFile: proto relpath, ImportPath: generated relpath}` for verified matches; skip failures silently

## Phase 4: Integration + Tests

- [ ] 4.1 Wire in `CheckService.Check()` at `internal/application/service.go`: construct `crosslanguage.New(config.CrossLanguage)` after loading config, append to detector list before calling `RunDetectors`
- [ ] 4.2 Unit tests in `crosslanguage/detector_test.go`: stem match, header verification, no-match, empty mappings, multiple mappings
- [ ] 4.3 Integration test: full `Check()` with temp dir containing `proto/order.proto` + `internal/pb/order.pb.go` with valid header, verify dep is emitted
- [ ] 4.4 `go vet ./...` + `go test -race ./...` — must pass clean
