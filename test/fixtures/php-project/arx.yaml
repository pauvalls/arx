version: "1.0"
layers:
  - name: domain
    paths:
      - "**/src/Domain/**"
  - name: application
    paths:
      - "**/src/Application/**"
  - name: infrastructure
    paths:
      - "**/src/Infrastructure/**"
rules:
  - id: domain-no-import-other
    from: domain
    to:
      - application
      - infrastructure
    type: Cannot
    severity: error
    explanation: Domain must not depend on other layers
  - id: application-no-import-infrastructure
    from: application
    to:
      - infrastructure
    type: Cannot
    severity: error
    explanation: Application should depend on interfaces, not implementations
